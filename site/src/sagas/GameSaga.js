// external
import {
  cancel,
  call,
  take,
  fork,
  put
} from 'redux-saga/effects'

// internal
import GameActions from '../redux/games'

export default function * flow (api) {
  console.log("THE FLOW IS CALLLLLED")
}
